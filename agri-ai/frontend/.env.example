# AgriAI Frontend Environment
# Copy to .env and adjust. NEVER put API keys or secrets here.
# All third-party API calls are proxied through the FastAPI backend.

# Base URL for the backend API
# When using the Vite dev proxy (default), leave this as /api
VITE_API_URL=/api
